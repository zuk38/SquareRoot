import React from 'react';
import { Typography } from '@mui/material';

const ProjectTitle = () => (
  //Project title
  <Typography
    color='textPrimary'
    variant='h1'
    sx={{
      ml: 2,
      mt: 5,
    }}
  >
    Tekna
  </Typography>
);

export default ProjectTitle;
