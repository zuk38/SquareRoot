import React from 'react';
import {
    Typography,
  } from '@mui/material';

const ProjectTitle = () => (
    //Project title
    <Typography
          color="textPrimary"
          variant="h1"
          sx={{
            ml: 1,
          }}
        >
          Tekna
        </Typography>
)

export default ProjectTitle;